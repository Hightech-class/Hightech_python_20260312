# This repository composition
* 본 문서 (README) : 파이썬의 주요 특징 및 식별자 유의사항 이론 정리
* [Identation.py](./Identation.py) : 들여쓰기 기본 원리 코드와 반복문(for), 조건문(if), 탈출(break) 실습 코드 포함.
---

### 📝 파이썬 코딩 관리 방법

#### 1. ⌨️ 들여쓰기 (Indentation)
* **방법:** `Tab` 키 사용 (파이썬 권장 표준은 **스페이스바 4칸**).
* **의미:** 파이썬에서 들여쓰기는 코드의 구역(블록)을 결정하는 **필수 문법**이다. 들여쓰기가 틀리면 코드가 실행되지 않거나 의도와 다르게 작동할 수 있다.

#### 2. ⚡ 주석 단축키 (`Ctrl` + `/`)
* **기능:** 선택한 코드 줄을 한꺼번에 주석 처리/해제(On/Off) 한다.
* **활용:** 함수(`def`)로 구역을 나누지 않은 여러 실습 코드들이 동시에 실행되어 충돌하거나 결과가 섞이는 것을 방지할 때 유용함.          
---
# 🐍 Hightech_python_20260311
## Python class contents
---

### 1. 파이썬 주요 특징
* **인터프리터 언어(스크립트 언어)** : 코드의 일부만을 실행시킬 수 있는 언어
* **동적 타입 언어** : 하나의 변수가 여러 가지 자료유형을 다룰 수 있는 언어(유연성)
* **플랫폼 독립적 언어** : 내가 작성한 프로그램이 OS의 영향을 받지 않음. 대부분 운영체계에서 모두 동작함(윈도우, 리눅스, 맥 등)

### 2. 파이썬 코드 작성 시 유의사항
* 파이썬 프로그래밍 시 소문자, 대문자, 숫자, `_`(underscore)로 조합 가능 (**대소문자 구분함**)
    * `_`(underscore)는 남발하지 않고 최대한 자제해서 쓰기 (프로그래밍에 혼란을 줄 수도 있음)
* **숫자로 시작해서는 안 됨** (위반: `1var` / 가능: `var1`)
* **키워드**(파이썬 문법의 식별자)를 식별자로 사용 X (예: `Global = 10`, `if = 5`)
    * *식별자: 무언가를 다른 것과 구분해서 부를 수 있는 변수*
* **특정 심벌 사용 X** (`!`, `@`, `#`, `$`, `%` 등)
* **식별자 길이 상관 X** (추천 방식: 단어 앞자리마다 대문자 섞기 / 예: `ThisIsALongVariable`)
* **한 줄에 하나의 명령문만 해석**
    * 길어질 경우 괄호, 역슬래시(`\`) 등으로 처리
    * 예시:
      ```python
      a = (1+2+3+
           4+5+6+
           7+8+9)
      ```

### 3. 파이썬 Identation, Comments
* **들여쓰기(Identation)**
    * 파이썬에서는 들여쓰기로 코드의 블록을 정의함
    * 일반적으로 4칸을 띄움 (tab 키 사용)
* **주석(Comments)**
    * 해쉬(hash) 기호 `#`으로 구성 (`'''` 또는 `"""` 기호로 다중 주석 처리)
    * *참고: 큰따옴표(`"""`)
